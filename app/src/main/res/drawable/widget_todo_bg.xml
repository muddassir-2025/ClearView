<?xml version="1.0" encoding="utf-8"?>
<!-- The To-Do widget's card. Same surface, stroke and radius as the Quran
     widget's, declared separately rather than shared so restyling one widget
     cannot silently restyle the other. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/widget_bg" />
    <corners android:radius="12dp" />
    <stroke
        android:width="1dp"
        android:color="@color/widget_stroke" />
</shape>
